# Git Operations Tool Configuration

# Repository URL (HTTPS format recommended: https://github.com/username/repo.git)
# SSH format also supported and will be converted: git@github.com:username/repo.git
GIT_REPO_URL=https://github.com/username/repository.git

# Local path where repository will be cloned/stored
GIT_REPO_PATH=/path/to/local/repo

# Default branch to use (main, master, develop, etc.)
GIT_DEFAULT_BRANCH=main

# GitHub Personal Access Token for authentication
# Generate at: https://github.com/settings/tokens
# Required scopes: repo (full control of private repositories)
# For public repos only: public_repo scope is sufficient
GITHUB_TOKEN=ghp_yourTokenHere

# GitHub Configuration (Optional)
# If you need to set git config values
GIT_USER_NAME=Your Name
GIT_USER_EMAIL=your.email@example.com

# ============================================
# GCP App Engine / Cloud Run Configuration
# ============================================

# GCP Project ID (required for Secret Manager)
GCP_PROJECT_ID=your-gcp-project-id

# Secret Manager secret names (for storing credentials securely)
# Recommended for production deployments
GITHUB_TOKEN_SECRET=github-token

# Storage configuration for GCP environments
# Set to true when running in App Engine/Cloud Run to use /tmp
USE_TMP_STORAGE=true
AUTO_CLEANUP=true
